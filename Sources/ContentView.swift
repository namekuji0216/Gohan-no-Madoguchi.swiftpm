import SwiftUI

struct ContentView: View {
    var body: some View {
        Button("OK") {
            // TODO: action
        }
        .buttonStyle(.borderedProminent)
        .controlSize(.large)
    }
}
